import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 32,
    paddingLeft: 32,
    paddingBottom: 32,
    backgroundColor: '#14171F',
  }
});
